How to run

# Terminal 1 — backend
cd club-connect/server
node server.js          # or: npm run dev (uses nodemon)

# Terminal 2 — frontend
cd club-connect/client
npm install             # only needed once
npm run dev             # opens at http://localhost:5173
